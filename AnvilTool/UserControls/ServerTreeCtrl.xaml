<UserControl x:Class="AnvilTool.UserControls.ServerTreeCtrl"
             x:Name="uCtrl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AnvilTool.UserControls"
             xmlns:properties="clr-namespace:AnvilTool.Properties" 
             mc:Ignorable="d"
             d:Background="White">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/AnvilTool;component/ResourceDictionary/CommonStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Image Grid.Column="0"
               Stretch="Uniform"
               Margin="4 0"
               VerticalAlignment="Center"
               Width="{StaticResource treeIconDim}"
               Source="{Binding Source={x:Static properties:Resources.fluxBg}, Converter={StaticResource ImageConverter}}"/>
        
        <TextBlock Grid.Column="1"
                   FontWeight="Bold"
                   VerticalAlignment="Center"
                   FontSize="{StaticResource treeLblFontSize}"
                   Text="{Binding Server.Name, ElementName=uCtrl}"
                   d:Text="Server 1"/>
    </Grid>
</UserControl>
