<UserControl x:Class="AnvilTool.UserControls.MoveButton"
             x:Name="uCtrl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AnvilTool.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <Button Content="{Binding Move.Label, ElementName=uCtrl}"
                Command="{Binding MoveClickCommand, ElementName=uCtrl}"
                CommandParameter="{Binding Move, ElementName=uCtrl}"
                x:Name="btn"
                MinWidth="75"
                Margin="2"/>
                <!--Command="{Binding DataContext.PressMoveCmd, RelativeSource={RelativeSource AncestorType=Window}}"-->
    </Grid>
</UserControl>
