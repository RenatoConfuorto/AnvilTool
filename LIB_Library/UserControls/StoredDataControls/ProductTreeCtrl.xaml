<UserControl x:Class="LIB.UserControls.StoredDataControls.ProductTreeCtrl"
             x:Name="uCtrl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LIB.UserControls"
             xmlns:properties="clr-namespace:LIB.Properties" 
             Padding="4 1"
             mc:Ignorable="d"
             d:Background="White">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/LIB;component/ResourceDictionary/CommonStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Image Grid.Column="0"
               Stretch="Uniform"
               VerticalAlignment="Center"
               Width="{StaticResource treeIconDim}"
               Source="{Binding Source={x:Static properties:Resources.hammerBg}, Converter={StaticResource ImageConverter}}"/>

        <Grid Grid.Column="1">

            <TextBlock Grid.Column="1"
                   Margin="20 0"
                   FontSize="{StaticResource treeLblFontSize}"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Stretch"
                   d:Text="Prodotto">
                <TextBlock.Text>
                    <MultiBinding StringFormat="{}{0} - {1}">
                        <Binding Path="Product.Name" ElementName="uCtrl"/>
                        <Binding Path="Product.Target" ElementName="uCtrl"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
        </Grid>

        <local:FinalSequence FinalSeq="{Binding Product.FinalSeq, ElementName=uCtrl, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            Margin="0 8"
                            Grid.Row="0"
                            Grid.Column="2"
                             VerticalAlignment="Center"
                            HorizontalAlignment="Center"/>
    </Grid>
</UserControl>
